# Founders Financial — Asset Allocation Dashboard
# Python dependencies
# Install locally with: pip install -r requirements.txt
# GitHub Actions installs these automatically

# HTTP requests + web session handling (BCA login)
requests>=2.31.0

# HTML parsing (BCA + NDR web scraping fallback)
beautifulsoup4>=4.12.0
lxml>=5.1.0

# PDF text extraction (BlackRock BII weekly PDF)
pymupdf>=1.23.0        # PyMuPDF — preferred, fast
pdfminer.six>=20221105 # fallback if pymupdf unavailable

# Headless browser (NDR JavaScript-rendered portal)
# GitHub Actions installs Chromium separately via:
#   playwright install chromium
playwright>=1.40.0

# Date utilities
python-dateutil>=2.8.2
